<UserControl x:Class="FondationBB.UCHistoriqueAdoptions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FondationBB"
             mc:Ignorable="d" d:DesignHeight="744" d:DesignWidth="1040"
             FontFamily="{StaticResource PoliceApp}">
    <Grid Background="White" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Historique des adoptions" Style="{StaticResource Titre}"/>
            <TextBlock x:Name="txtTotal" Style="{StaticResource Secondaire}" Margin="0,4,0,0"/>
        </StackPanel>

        <DataGrid Grid.Row="1" x:Name="grilleAdoptions" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="120"/>
                <DataGridTextColumn Header="Animal" Binding="{Binding Animal}" Width="*"/>
                <DataGridTextColumn Header="Adoptant" Binding="{Binding Adoptant}" Width="*"/>
                <DataGridTextColumn Header="Contact" Binding="{Binding Contact}" Width="200"/>
                <DataGridTextColumn Header="Frais" Binding="{Binding Frais}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
