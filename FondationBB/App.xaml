<Application x:Class="FondationBB.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:FondationBB"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Couleurs de l'application -->
        <SolidColorBrush x:Key="BrushPrimary" Color="#185FA5"/>
        <SolidColorBrush x:Key="BrushPrimaryDark" Color="#0C447C"/>
        <SolidColorBrush x:Key="BrushPrimaryLight" Color="#E6F1FB"/>
        <SolidColorBrush x:Key="BrushText" Color="#1A1A1A"/>
        <SolidColorBrush x:Key="BrushTextSec" Color="#6B7280"/>
        <SolidColorBrush x:Key="BrushPanel" Color="#F5F5F0"/>
        <SolidColorBrush x:Key="BrushBorder" Color="#E5E5E0"/>
        <SolidColorBrush x:Key="BrushWhite" Color="#FFFFFF"/>

        <FontFamily x:Key="PoliceApp">Segoe UI, Arial</FontFamily>

        <!-- Titre de page -->
        <Style x:Key="Titre" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
        </Style>

        <!-- Texte secondaire -->
        <Style x:Key="Secondaire" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{StaticResource BrushTextSec}"/>
        </Style>

        <!-- Libellé de champ -->
        <Style x:Key="LabelChamp" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{StaticResource BrushTextSec}"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <!-- Champ de saisie : simples setters, template par défaut (plus de bug d'alignement) -->
        <Style x:Key="Champ" TargetType="TextBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- Liste déroulante -->
        <Style x:Key="ChampCombo" TargetType="ComboBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- Bouton principal : aplat coloré (template trivial, sans trigger) -->
        <Style x:Key="BoutonPrimaire" TargetType="Button">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource BrushPrimary}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Bouton secondaire : contour -->
        <Style x:Key="BoutonSecondaire" TargetType="Button">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource BrushPrimary}"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6"
                                BorderBrush="{StaticResource BrushBorder}" BorderThickness="1"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Carte / panneau -->
        <Style x:Key="Carte" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
        </Style>
    </Application.Resources>
</Application>
